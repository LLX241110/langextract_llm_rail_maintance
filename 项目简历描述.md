# 《基于 LangExtract 与本地 LLM 的钢轨伤损运维知识图谱构建项目》 2025.09–2025.11

- **项目概述**：面向钢轨伤损养护场景，整合养护规范、维修手册与历史处理记录等非结构化文本，构建端到端的运维知识抽取与图谱化管理流程。

- **核心技术链路**：非结构化文本 → LangExtract 信息抽取 → LLM 结构化抽取 → 实体对齐/去重 → Neo4j 图谱 → 问答辅助。

  - 设计 Schema 驱动的信息抽取流程，定义伤损类型、维修方式、磨耗限值、维修周期等实体与关系模式，结合 LLM 实现专业文本结构化解析。
  - 基于规则匹配与字符串相似度完成实体去重与术语对齐，将清洗后的知识写入 Neo4j，构建多实体关系图谱并支持 Cypher 高效查询。
  - 实现 LLM 调用 Trace 机制，基于 LangChain `BaseCallbackHandler` 自动记录每次调用的 step/model/latency/tokens/cost，输出 JSONL 日志，支持费用估算与性能分析。
  - 设计 Checkpoint 断点续跑方案，各步骤中间结果通过 Pydantic 序列化落盘，支持 `--resume` 从失败步骤恢复，避免 LLM 重复调用。

- **成果输出**：支持钢轨打磨工艺、磨耗限值查询及维修建议等典型问答，可观测 LLM 调用链路，提升知识检索效率与系统可维护性。
